-- Добавление поля full_name в таблицу users (если его еще нет)
-- SQLite не поддерживает IF NOT EXISTS для ALTER TABLE ADD COLUMN
-- Поэтому используем проверку через PRAGMA table_info
-- Эта миграция будет выполняться только если колонка не существует

